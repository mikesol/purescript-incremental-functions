{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build && pulp test"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript": "^0.14.3",
    "purescript-psa": "^0.5.0",
    "rimraf": "^2.5.4",
    "spago": "^0.20.3"
  }
}
